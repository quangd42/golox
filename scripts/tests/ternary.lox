fn add(a, b) {
	return a + b;
}

fn sayHi(first, last) {
  print "Hello, " + first + " " + last + "!";
}

sayHi("Darkness", add(1, 5) <= 6 ? "My Old Friend" : "I'm ready");

// truthy
print "true" ? "t'is true" : "not";
